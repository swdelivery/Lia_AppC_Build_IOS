import { generateActionTypes } from "@Redux/helper";

export const CHECK_FLASH_SALE = generateActionTypes(
  "@flash-sale/check-flash-sale"
);

export const GET_CURRENT_FLASH_SALE_SERVICES = generateActionTypes(
  "@flash-sale/get-current-flash-sale-services"
);
